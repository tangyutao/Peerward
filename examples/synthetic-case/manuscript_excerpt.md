# Synthetic manuscript excerpt

We consider fictional agents connected by a communication graph. The network minimizes the sum of differentiable local objective functions. Each agent stores its own objective and exchanges an auxiliary state with its neighbors.

The proposed algorithm is distributed and robust. A numerical example demonstrates convergence.

